# Latest user priority — 2026-09-10

SAMA reproduction deferred by explicit user request. Its waiting supervisor
PID2005083 stopped while WAITING_FOR_IDLE_GPU, no SAMA GPU training started.
Files/isolated environment retained; do not resume its automatic launcher.

Keep current W128D20 GT scratch PID2004698 and W128D10 detected input PID2027023
running to their registered80epochs. New priorities after current jobs:

1. Conditioning-location ablations on W64D8/~0.8M: Delta-only and B/C-only,
   each80epochs, same registered seed/recipe as Full. First map existing none/
   Full controls and verify provenance; rerun controls only if not comparable.
   Feature-only injection is not automatically the no-conditioning control.
   Unconditioned parameters derive from original features, never zero them.
   Preserve recurrent content, projection tensors and graph structure. Add routing
   equality/gradient tests and actual Full numerical equivalence before launch.
   Configs/routing implementation remain pending, not claimed complete.
2. W128D10 GT2D FRESH seed0,80epochs,batch8 using the generated balanced GT config.
   This is newly reauthorized GT work after earlier target correction; create a NEW
   experiment identity/output. Do not resume the cancelled zero-epoch D10 GT attempt.
3. Previously planned seed repeats remain last; no new SAMA job ahead of these.

GPU preflight and exact fresh-state/config hashes required for every launch.
No new third simultaneous job under current ~91% VRAM usage. If spare capacity
appears as a current job completes, reassess before scheduling. App recurring
monitoring stays disabled; this is priority registration, not an active scheduler.
