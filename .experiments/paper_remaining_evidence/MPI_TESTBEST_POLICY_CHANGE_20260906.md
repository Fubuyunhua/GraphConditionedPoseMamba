# MPI evaluation policy amendment — user instruction

The user explicitly requested the H36M-style policy: evaluate every epoch and select the best model. Both MPI Full and the future released-A0 baseline now use `mpi3dhp_checkpoint_selection: legacy_test_best` through new named configs. Primary is minimum test-monitored EMA MPJPE across epochs1-120; paired P-MPJPE/PCK150/AUC must come from that same checkpoint. Raw best is tracked separately, never substituted for primary EMA. Fixed120 raw/EMA remains secondary.

This is an explicit mid-run protocol amendment, not a locked-test or unbiased-validation result. Preserve the previous fixed-only config and logs. Dataset, model, losses, optimizer, LR, EMA, batch and total120-epoch budget are unchanged. The existing runner already supports per-epoch raw/EMA evaluation and conditional best checkpoint writes. Only its policy log message was clarified; no algorithm code changed.

Full had completed epoch1 when this instruction arrived. Its partial epoch2 was stopped at05:53:19UTC; the other corrected-A0 job was untouched. Preserve original raw/EMA checkpoint and metadata under verification/policy_change_20260906. Both epoch1 raw and EMA were strictly loaded and evaluated, so epoch1 participates in the best selection. Resume from the epoch1 raw state, including optimizer/EMA/LR/RNG; populate only best-metric metadata using those evaluations. Seeded epoch1 best files are stored in a new run directory, runs/mpi_full_testbest_seed0. Subsequent metrics.json begins at epoch2; epoch1 evaluations remain separate auditable artifacts. Do not claim metrics history starts from random initialization in the new directory alone.

Gate: both evaluations finite, checkpoint epoch/type/optimizer/RNG/EMA present, inherited config differs only in config/name/path/selection metadata. The first gate rejected the automatically derived config `name` field; it was added to the metadata allowlist before any continuation step. Model/scientific fields were not relaxed. CPU policy-dispatch test passed. Original CUDA/data gates remain applicable because training computations did not change. The first resumed batches must be observed before recording RUNNING.

Monitoring must use the new log launch_logs/mpi_full_testbest_seed0.log, checkpoint directory runs/mpi_full_testbest_seed0 and exit marker verification/testbest_exit_code.txt. Old formal_exit_code.txt records the intentional policy-transition stop and must not trigger a retry or failure alarm. Do not resume the old fixed-only launcher. Continue authorized concurrency with corrected-A0, no third run. All future MPI A0 training uses configs/pose3d_3dhp/posemamba_3dhp_w64_d6_m1_released_testbest.yaml and the same primary checkpoint rule.
