# GT2D fine-tuning and seeds-last amendment

User explicitly moves different-seed experiments last. Preserve current MPI and small-GT jobs, then128 detected-input80e ->256 detected-input60e ->128 GT2D fine-tune30e ->256 GT2D fine-tune30e -> six H36M A0/A2/Full seed1/2 repeats. Old repeat-first or GT-from-scratch budgets in the historical plan are superseded.

128 and256 have different tensor dimensions and layer counts. Do not load128 weights into256 with strict=False, pad/slice weights, distill or invent an architecture migration. Each GT model starts from the same-width detector model's best EMA selected under its registered policy. Detector training remains fresh, with the25636.x target unchanged. GT scores cannot substantiate detector36.x.

GT settings:30 epochs,peak LR3e-5,3-epoch warmup from3e-6,cosine to3e-6,batch4,all layers trainable. Preserve same-width architecture/DropPath/loss/WD/EMA decay. Per-epoch monitored-best EMA P1/P2; fixed30 secondary. This is transfer learning, not a from-scratch input-only comparison; report detector pretraining budget plus GT adaptation budget. Fine-tuning is expected to save training time, not guaranteed to outperform fresh training.

Launch uses finetune=true with --pretrained pointing to a newly prepared initializer directory and --selection gt2d_init_ema.bin. Never use --resume for this stage. Prepare an immutable weights-only export from verified same-width best_ema_epoch.bin with model_pos,checkpoint_type=ema,ema_updates=0; retain original checkpoint untouched and record SHA256 of source/export and exact tensor equality. Existing train.py loads strict=True, starts a fresh optimizer/scheduler at epoch0, and initializes EMA shadow from the loaded EMA weights. Do not pass source optimizer/RNG/old update count to GT adaptation. Explicit CLI path matters: train.py consumes opts.pretrained and opts.selection, not only YAML paths.

Before PASS verify actual config inheritance, strict source load, exact weight equality and full parameter coverage, optimizer has no restored moments, EMA shadow equals initialized weights/count0, start epoch0 and schedule30e; GT coordinate/flip/root protocol and actual finite-gradient B4 training/lifecycle gate. Source checkpoints do not exist until detector training completes; that is a dependency, not missing user approval. Preserve current running jobs. On failure diagnose rather than automatically falling back to a different source or random initialization.
