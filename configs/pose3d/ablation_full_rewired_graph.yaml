_base_: graph_posemamba_h36m_w64_d8_0p8m_memopt_speed.yaml

config: configs/pose3d/ablation_full_rewired_graph.yaml
checkpoint: runs/paper_remaining_evidence/full_rewired_graph_seed0
pretrained: runs/paper_remaining_evidence/full_rewired_graph_seed0
seed: 0
epochs: 80

# Match the actual legacy A2/Full optimizer path: warmup_epochs was declared
# historically but was not enabled in the registered 80-epoch runs.
enable_linear_warmup: false
honor_no_weight_decay: false
max_grad_norm: 0.0
track_parameter_update_norm: false

graph_injection_mode: control
graph_conditioned_ssm: true
graph_topology_mode: degree_preserving_rewired
graph_rewire_seed: 3407
recurrence_scope: independent

